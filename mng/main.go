package main

import (
    "fmt"
    "os"
)

func main() {
    args := os.args

    if len(args) == 1 {
        //check args
        // new contanier:
            //allocate less rescources to all containers
        // deleted container:
            //allocate more rescources to all containers

        //calculate based off machine specifications

        
    }
sl
}